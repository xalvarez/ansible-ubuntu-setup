---
- name: Install apt packages
  apt:
    name: "{{ packages }}"
    force_apt_get: yes
  vars:
    packages:
    - openvpn
    - python3.7
    - python3.7-dev
    - python3-pip
    - jq
    - cifs-utils
    - keyutils
