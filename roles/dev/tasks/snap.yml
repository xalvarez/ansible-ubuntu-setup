---
- name: Install classic snaps
  snap:
    name: "{{ item }}"
    classic: yes
  loop:
      - code

- name: Install non-classic snaps
  snap:
    name: "{{ item }}"
    classic: yes
  loop:
      - insomnia

- import_tasks: visual_studio_code.yml
  become: no
